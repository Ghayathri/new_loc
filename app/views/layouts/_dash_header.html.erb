<nav class="navbar navbar-expand-lg bg-info">
    <div class="container">
        <div class="navbar-translate">
           <%= link_to "League of Champions" , root_path, method: :get, class: "navbar-brand" %>
            <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navigation" aria-controls="navigation-index" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-bar bar1"></span>
            <span class="navbar-toggler-bar bar2"></span>
            <span class="navbar-toggler-bar bar3"></span>
        </button>
        </div>
        <div class="collapse navbar-collapse justify-content-end" id="navigation">
            <ul class="navbar-nav">
                <li class="nav-item <%= request.fullpath.include?("dashboards") ? 'active' : '' %>">
                    <a class="nav-link" href="#pablo">
                        Dashboard
                    </a>
                </li>
                
                <li class="nav-item <%= request.fullpath.include?("matches") ? 'active' : '' %>">
                    <%= link_to matches_path, class: "nav-link" do %>
                        <!-- <img src="/assets/Squad.png"> --> Matches
                    <% end %>
                </li>
                <li class="nav-item <%= request.fullpath.include?("matches") ? 'active' : '' %>">
                    <%= link_to squads_path, class: "nav-link" do %>
                        <!-- <img src="/assets/Squad.png"> --> Squads
                    <% end %>
                </li>
                <li class="nav-item">
                    <%= link_to destroy_user_session_path, method: :delete, class: "nav-link" do %>
                        <i class="fas fa-sign-out-alt"></i>
                    <% end %>
                </li>
            </ul>
        </div>
    </div>
</nav>